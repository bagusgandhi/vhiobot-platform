version: '3'

services:
  vhiobot-platform:
    container_name: vhiobot-platform
    build: 
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - 8082:3000
    env_file:
      - .env.local